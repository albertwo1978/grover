name: test

on: 
  push:
    branches:
      - master  

jobs:
  blah:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        cd Terraform
        ls
        terraform graph -h
